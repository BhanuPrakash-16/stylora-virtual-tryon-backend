# Stylora Backend Environment Configuration
# ==========================================

# REQUIRED: Replicate API Token
# Get your token from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Safety Thresholds (Optional - defaults in config.py)
AGE_CHECK_ENABLED=true
MIN_POSE_CONFIDENCE=0.5
SKIN_EXPOSURE_THRESHOLD=0.25

# Notes:
# - REPLICATE_API_TOKEN is REQUIRED for virtual try-on to work
# - Use Replicate free trial credits for testing
# - All other settings have sensible defaults
